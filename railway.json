{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "apt-get update -y && apt-get install -y ffmpeg && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start",
    "restartPolicyType": "on-failure",
    "healthcheckPath": "/"
  }
}
